# Microwave Cathode Full-Fluid Plasma Code

This repository contains a full-fluid plasma simulation code for a microwave cathode based on a non-neutral drift-diffusion (NDD) model.

The code is intended for numerical analysis of plasma transport, electrostatic potential, microwave energy coupling, and related plasma-fluid behavior in a microwave cathode system.

## Overview

This code solves a full-fluid plasma model for a microwave cathode.

The model is based on a non-neutral drift-diffusion formulation and includes fluid equations for charged and neutral species, electrostatic field calculation, microwave-related source terms, and related transport models.

The current implementation is written in C++ and uses external numerical libraries for linear solvers and YAML-based parameter input.

## Features

- Full-fluid plasma simulation for a microwave cathode
- Non-neutral drift-diffusion (NDD) model
- Ion, electron, and neutral-species transport modeling
- Electrostatic potential calculation
- Microwave power coupling parameters
- Secondary electron emission settings
- Restart file support
- YAML-based setup file
- CSV-based input data files
- CSV-based result output files
- Python scripts for post-processing and visualization

## Directory Structure

The current repository structure is:

```text
.
├── README.md
├── Makefile
├── a.out
├── include/
│   ├── common.hpp
│   └── functions.hpp
├── src/
│   ├── EMfield.cpp
│   ├── common.cpp
│   ├── fluid.cpp
│   ├── initial.cpp
│   ├── input.cpp
│   ├── main.cpp
│   ├── output.cpp
│   ├── solver.cpp
│   └── support.cpp
├── input/
│   ├── setup.yaml
│   ├── Bfield_data.csv
│   ├── distance.csv
│   ├── coefEISEE.csv
│   ├── rateCoef_e.csv
│   └── rateCoef_i.csv
├── restart/
│   └── (restart.csv)
├── results/
│   ├── (residuals.csv)
│   ├── (restart*.csv)
│   ├── (result*.csv)
│   ├── (current*.csv)
│   └── (result_deg*.csv)
└── scripts/
```

## Main Directories

### `src/`

This directory contains the main C++ source files.

Typical roles are:

- `main.cpp`: main program
- `input.cpp`: input parameter and data loading
- `initial.cpp`: initialization
- `fluid.cpp`: fluid model calculation
- `EMfield.cpp`: electromagnetic or electric-field-related calculation
- `solver.cpp`: numerical solver routines
- `output.cpp`: output routines
- `common.cpp`, `support.cpp`: common utilities and support functions

### `include/`

This directory contains header files.

```text
include/
├── common.hpp
└── functions.hpp
```

### `input/`

This directory contains setup files and input data files required for the simulation.

```text
input/
├── setup.yaml
├── Bfield_data.csv
├── distance.csv
├── coefEISEE.csv
├── rateCoef_e.csv
└── rateCoef_i.csv
```

The main setup file is:

```text
input/setup.yaml
```

Other CSV files are used as input tables, such as magnetic-field data, distance data, reaction rate coefficients, and secondary electron emission coefficients.

### `restart/`

This directory contains restart data.

```text
restart/restart.csv
```

Restart files are used to resume simulations from a previous state.

### `results/`

This directory contains simulation output files.

Typical files include:

```text
results/
├── residuals.csv
├── restart*.csv
├── result*.csv
├── current*.csv
└── result_deg*.csv
```

These files are generated by the simulation and are usually not tracked by Git, except for small sample outputs if needed.

### `scripts/`

This directory contains Python scripts and command files for post-processing and visualization.

## Requirements

The code requires the following libraries and tools:

- C++ compiler with C++17 support
- MPI
- HYPRE
- yaml-cpp
- Python for post-processing scripts

On macOS with Homebrew, these libraries may be installed using:

```bash
brew install open-mpi hypre yaml-cpp
```

## Build

Compile the code using:

```bash
make
```

or, depending on the Makefile target:

```bash
make Normal
```

The current executable file is generated as:

```text
a.out
```

## Run

Run the simulation using:

```bash
./a.out
```

The setup file is read from:

```text
input/setup.yaml
```

Before running the code, make sure that `input/setup.yaml` exists and contains all required parameters.

## Setup File

The simulation parameters are defined in:

```text
input/setup.yaml
```

The setup file is written in YAML format. Each parameter is grouped by category, while the parameter names are kept identical to the corresponding C++ variable names.

Example:

```yaml
microwave:
  Pmw: 8.0                # (double) Microwave power [W]
  omegam: 26703537556e0   # (double) Microwave angular frequency

scheme:
  icon_PC: 1              # (int) Whether to use the PC method
  dt_ini: 1.0e-10         # (double) Initial time step
```

The basic rule is:

```text
category:
  C++_variable_name: value
```

The category is used only for readability and organization. The parameter name itself should correspond one-to-one with the variable name used in the C++ code.

## Main Parameter Categories

The setup file contains the following categories:

- `microwave`: microwave power, frequency, and related parameters
- `plasma`: plasma and neutral particle parameters
- `material`: dielectric material properties
- `bias`: bias voltage settings
- `neutral_inlet`: neutral gas inlet conditions
- `transport_model`: transport model switches and coefficients
- `see`: secondary electron emission parameters
- `scheme`: numerical scheme and time-step settings
- `solver`: solver convergence criteria and iteration settings
- `microwave_coupling`: microwave calculation interval
- `relaxation`: under-relaxation factors
- `simulation`: global simulation settings
- `output`: output settings

## Input Data Files

In addition to `setup.yaml`, the code uses several CSV input files in the `input/` directory.

Current input files include:

- `Bfield_data.csv`
- `distance.csv`
- `coefEISEE.csv`
- `rateCoef_e.csv`
- `rateCoef_i.csv`

These files should be kept in `input/` because they are required to reproduce the simulation setup.

## Output Files

The simulation writes output files to the `results/` directory.

Current output examples include:

- `result0.csv`
- `result_deg0.csv` to `result_deg17.csv`
- `current0.csv`
- `residuals.csv`
- `restart0.csv`

The exact meaning of each output file should be documented as the code is developed.

## Restart Files

Restart files are used to resume simulations from previously saved states.

Current restart file:

```text
restart/restart.csv
```

Restart files can become large, so they are usually excluded from Git unless a small sample restart file is intentionally kept.

## Post-processing

Post-processing scripts are placed in:

```text
scripts/
```

Typical usage may be:

```bash
python scripts/plot_phi.py
python scripts/plot_rhoe.py
python scripts/plot_Te.py
```

Some command files are also included:

```text
scripts/plot_main.command
scripts/plot_main_mini.command
```

TODO: Add detailed instructions for each plotting script.

## Version Control Policy

The following files should be tracked by Git:

- source files in `src/`
- header files in `include/`
- setup files in `input/`
- input CSV files required for reproduction
- `Makefile`
- `README.md`
- post-processing scripts in `scripts/`

The following files should usually not be tracked:

- executable files such as `a.out`
- object files
- generated result files in `results/`
- large restart files in `restart/`
- temporary files

Recommended `.gitignore` entries:

```gitignore
# Executables and compiled files
a.out
*.out
*.o
*.exe

# Results
results/*
!results/.gitkeep

# Restart files
restart/*
!restart/.gitkeep

# OS and editor files
.DS_Store
.vscode/
```

If `results/` and `restart/` should remain visible on GitHub even when their contents are ignored, place empty `.gitkeep` files in those directories.

## Notes for Developers

When adding new setup parameters:

1. Add the parameter to `input/setup.yaml`.
2. Use the same name as the corresponding C++ variable.
3. Add the parameter-reading line in `inputParam()`.
4. Specify the type in the YAML comment, such as `(int)` or `(double)`.
5. Check that the parameter is correctly loaded at runtime.

Example YAML entry:

```yaml
scheme:
  new_parameter: 1.0   # (double) Description of the new parameter
```

Corresponding C++ side:

```cpp
category = "scheme";
READ_DOUBLE(category, new_parameter);
```

## License

TODO: Add license information.

Commercial use is permitted if the selected license allows it.

Recommended permissive license options:

- MIT License
- BSD 3-Clause License
- Apache License 2.0

For a simple permissive license, the MIT License is commonly used.

## Citation

TODO: Add citation information.

If this code is used in academic work, please cite the related paper, thesis, or repository.

Example:

```text
Ryo Shirakawa, "TODO: Title of related work", TODO: Journal/Conference/Thesis, TODO: Year.
```

## Author

Ryo Shirakawa

TODO: Add affiliation, laboratory, or contact information if needed.

## Acknowledgements

TODO: Add acknowledgements for supervisors, collaborators, funding, or related software libraries.
